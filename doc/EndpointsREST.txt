

Administradores (HU-A2)

GET /api/administradores?q=

POST /api/administradores

PUT /api/administradores/{id}

DELETE /api/administradores/{id} (activo=false)

Centros (HU-A3)

GET /api/centros-salud?nombre=&localidad=

POST /api/centros-salud

PUT /api/centros-salud/{id}

DELETE /api/centros-salud/{id}

Médicos (HU-A4 + HU-M6)

GET /api/medicos?nombre=&localidad=&centroId=

POST /api/medicos (password inicial = numeroColegiado)

PUT /api/medicos/{id}

DELETE /api/medicos/{id}

Pacientes (HU-A5 + HU-P5)

GET /api/pacientes?nombre=&localidad=&centroId=&medicoId=

POST /api/pacientes (password inicial = dni)

PUT /api/pacientes/{id}

DELETE /api/pacientes/{id}

Farmacias (HU-A6 + HU-F4)

GET /api/farmacias?nombreEstablecimiento=&localidad=

POST /api/farmacias (password inicial = numColegiadoFarmaceutico)

PUT /api/farmacias/{id}

DELETE /api/farmacias/{id}

Medicamentos (HU-A8 + HU-M4)

GET /api/medicamentos?nombre=&principioActivo=&fabricante=&familia=

POST /api/medicamentos

PUT /api/medicamentos/{id}

DELETE /api/medicamentos/{id}

Citas (HU-A7 + HU-M2 + HU-P2 + HU-P3)

GET /api/citas?fecha=&medicoId=&pacienteId= (listado/filtrado)

GET /api/citas/huecos?fecha=YYYY-MM-DD&medicoId= (HU-P3)

POST /api/citas (crear)

PATCH /api/citas/{id} (anular/completar/ausente)

Atención de cita / Prescripciones (HU-M3..M5)

GET /api/citas/{id}/atencion (paciente + prescripciones en vigor)

POST /api/prescripciones (crea prescripción + genera plan recetas)

PATCH /api/prescripciones/{id} (anular: activa=false + recetas ANULADA)

Recetas (HU-P4 + HU-F2 + HU-F3)

GET /api/recetas?pacienteId= (mis recetas pendientes)

GET /api/recetas?tarjetaSanitaria= (farmacia consulta)

PATCH /api/recetas/{id} (servir)
---------------
viejos
Administrador
    quitar /admins
    GET /api/administradores?q= (opcional) listado
    POST /api/administradores
    PUT /api/administradores/{id}
    DELETE /api/administradores/{id} (activo=false)
Centros de salud
    GET /api/centros-salud?nombre=&localidad= 
    POST /api/centros-salud
    PUT /api/centros-salud/{id}
    DELETE /api/centros-salud/{id} (en patch no poner verbs, mejor delete)
Médicos
    GET /api/medicos?nombre=&localidad=&centroId=
    POST /api/medicos (password inicial = numColegiado)
    PUT /api/medicos/{id} 
    DELETE /api/medicos/{id}
Pacientes
    GET /api/pacientes?nombre=&localidad=&centroId=&medicoId=
    POST /api/pacientes 
    PUT /api/pacientes/{id}
    DELETE /api/pacientes/{id}
Farmacias
    GET /api/farmacias?nombreEstablecimiento=&localidad=
    POST /api/farmacias 
    PUT /api/farmacias/{id}
    DELETE /api/farmacias/{id}
Citas 
    citas libres  GET /api/citas?fecha=YYYY-MM-DD&medicoId=
    GET /api/citas?fecha=YYYY-MM-DD&medicoId=&pacienteId= 
    PATCH /api/citas/{id} (estado=ANULADA)
Medicamentos
    GET /api/medicamentos?nombre=&principioActivo=&fabricante=&familia= 
    POST /api/medicamentos
    PUT /api/medicamentos/{id}
    DELETE /api/medicamentos/{id}
Agenda del día
    GET /api/medico/agenda?fecha=YYYY-MM-DD 
    PATCH /api/citas/{medicoId}
Atención de cita, api/citas pudee tenr recurso medico
    GET /api/citas/atencion?medicoId= 
    PATCH /api/prescripciones/{medicoId}
    Crear prescripción + plan de recetas
     POST /api/prescripciones
Ls prescrip en api/prescrip
Perfil
    PUT /api/medico/perfil 
    PUT /api/medico/perfil/password 
Mis citas
    GET /api/citas?pacienteId= , implemetar en el controlador de las citas
    PATCH /api/citas/{id}
Nueva cita, gestionar nuevas citas a partir de citas directamente no paciente
    GET /api/citas/huecos?fecha=YYYY-MM-DD 
    POST /api/citas/{pacienteId} 
    GET /api/citas?pacienteId= 
Mis recetas
    GET /api/recetas/{pacienteId} 
    
Perfil
    PUT /api/paciente/{id}
Gestión receta, rectas igual que nueva cita, recurso de 1º nivel
    GET /api/recetas?tarjetaSanitaria=TS123 
    PATCH /api/recetas/{id} patch or put dpendiendo del contexto

